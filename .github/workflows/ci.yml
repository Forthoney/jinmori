name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout main repo
        uses: actions/checkout@v4

      - name: Install MLton
        run: |
          sudo apt-get update
          sudo apt-get install -y mlton

      - name: Build main project
        run: |
          yes | bash bootstrap.sh

      - name: Download try
        run: |
          curl -LO https://github.com/binpash/try/archive/refs/tags/v0.2.0.tar.gz
          tar -xzf v0.2.0.tar.gz
          try-v0.2.0/try echo 'try successful'

      - name: Run tests
        run: |
          TRY=try-v0.2.0/try test/cli/new/runner
