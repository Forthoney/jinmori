#!/bin/sh

usage() {
	cat <<EOF
A Standard ML package manager

Usage: jinmori [subcommand]
EOF
}

update_path() {
	shell_config="$HOME/.bashrc"
	if [ -n "$ZSH_VERSION" ]; then
		shell_config="$HOME/.zshrc"
	fi
	start_mark="# <<< Autogenerated by jinmori"
	end_mark="# >>> Autogenerated by jinmori"

	if grep -xqF "$start_mark" "$shell_config"; then
		return
	fi

	cat >>"$shell_config" <<EOF

$start_mark

case ":\$PATH:" in
*:$HOME/.jinmori/bin:*)
	;;
*)
	export PATH=$HOME/.jinmori/bin\${PATH:+:\${PATH}}
	;;
esac

$end_mark
EOF
}

CMD_NAME="jinmori"
CMD_PATH="$0"

case "$1" in
setup)
	shift
	DIR="$HOME/.jinmori"
	FORCE=0
	PATHAPPEND=1

	if ! OPTIONS=$(getopt --options d:f --longoptions dest:,force,set-env -- "$@"); then
		usage
		exit 1
	fi
	eval set -- "$OPTIONS"

	while true; do
		case "$1" in
		-d | --dest)
			DIR="$2"
			shift 2
			;;
		-f | --force)
			FORCE=1
			shift
			;;
		--no-modify-path)
			PATHAPPEND=0
			shift
			;;
		--)
			shift
			break
			;;
		*)
			echo "Unknown option '$1'" >&2
			usage
			exit 1
			;;
		esac
	done

	tmp_path=""
	if [ -d "$DIR" ]; then
		if [ "$FORCE" -eq 1 ]; then
			rm -rf "$DIR"
		else
			printf "Directory %s already exists. You may have initialied %s already." "$DIR" "$CMD_NAME"
			printf "Do you want to delete the existing directory? (y/N) "

			read -r response
			case "$response" in
			[yY][eE][sS] | [yY])
				tmp_path=$(mktemp)
				cp -p -- "$CMD_PATH" "$tmp_path"
				CMD_PATH="$tmp_path"
				rm -rf "$DIR"
				;;
			*)
				echo "Aborting. $DIR was not modified." >&2
				exit 1
				;;
			esac
		fi
	fi

	mkdir -p "$DIR/bin" "$DIR/deps"
	echo "Initialized jinmori directory at $DIR" >&2
	cp -- "$CMD_PATH" "$DIR/bin/$CMD_NAME"
	if [ -n "$tmp_path" ]; then
		rm --force "$tmp_path"
	fi

	if [ "$PATHAPPEND" = 1 ]; then
		update_path
	fi
	;;

new)
	shift
	case "$1" in
	"")
		echo "No project name provided." >&2
		exit 1
		;;
	*[!A-Za-z]*)
		echo "Project name must only contain ASCII alphabets." >&2
		exit 1
		;;
	*)
		PROJ="$1"
		shift

		src="$PROJ/src"
		tests="$PROJ/tests"
		bin="$PROJ/bin"
		main=$(echo "$PROJ" | awk '{ $1 = toupper(substr($1, 1, 1)) substr($1, 2); print }')

		if ! mkdir -p "$src" "$tests" "$bin" 2>/dev/null; then
			echo "Destination \`$PROJ\` already exists." >&2
			exit 1
		fi

		cat >"$src/$main.sml" <<EOF
structure $main:
sig
  val greeting: string
end =
struct
  val hello = "Hello,"
  val world = "World!"
  val greeting = hello ^ world ^ "\n"
end
EOF

		cat >"$src/Exec.sml" <<EOF
val () = print $main.greeting
EOF

		cat >"$tests/$main.sml" <<EOF
if true = false then
	raise Fail "The fabric of reality crumbles..."
else
	()
EOF

		cat >"$src/src.mlb" <<EOF
\$(SML_LIB)/basis/basis.mlb
$main.sml
EOF

		cat >"$PROJ/$PROJ.mlb" <<EOF
src/src.mlb
src/Exec.sml
EOF

		cat >"$PROJ/$PROJ.tests.mlb" <<EOF
src/src.mlb
tests/$main.sml
EOF

		cat >"$PROJ/Makefile" <<EOF
BIN := \$(CURDIR)/bin
SRC := \$(CURDIR)/src
TESTS := \$(CURDIR)/tests

APP := \$(BIN)/$PROJ
TESTER := \$(BIN)/testall

MLTON_FLAGS := $MLTON_FLAGS

all: \$(APP) \$(TESTER)
.PHONY: test

\$(APP): \$(SRC)/*.sml
	mlton \$(MLTON_FLAGS) -output \$@ $PROJ.mlb

\$(TESTER): \$(SRC)/*.sml \$(TESTS)/*.sml
	mlton \$(MLTON_FLAGS) -output \$@ $PROJ.tests.mlb

test: \$(TESTER)
	\$(TESTER)

clean:
	rm -f \$(BIN)/*
EOF
		;;
	esac
	;;

help | --help | -h | "")
	usage
	;;
*)
	echo "Error: Unknown command or flag '$SUBCOMMAND'" >&2
	usage
	exit 1
	;;
esac
